# 框架架构

沿用参考项目的分层：Web 页面 → HTTP 接入 → 共享运行时 → 适配器。CLI 和 MCP 共用该运行时的清单读取逻辑。

当前只有空白页面、空能力清单、HTTP 健康检查与发现、CLI 诊断、MCP 只读 manifest。适配器目录留空，不保留任何领域工具、后台工具服务、外部 API 连接器或示例数据。

UI 不设置产品名称、图标、导航内容或视觉品牌。功能、数据模型、任务生命周期和执行协议在后续需求明确后设计。

技术基础：React、TypeScript、Vinext/Vite、Node.js；保留本地构建所需配置。未配置云端站点、数据库或部署资源。
