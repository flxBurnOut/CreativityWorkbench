# 全类型连续创作验收记录

日期：2026-09-08。对应 [实施说明](CONTINUOUS_WORKFLOW.md)。原有文化与系统规则保持有效。

## 自动检查

| 检查 | 最终结果 |
| --- | --- |
| npm test | 73 项：71 通过，2 项既有 Windows 环境失败 |
| npm run lint | 通过 |
| npm run typecheck | 通过 |
| npm run build | 通过；vinext 的首页静态分类仍提示 Unknown |
| git diff --check | 通过；仅有 Git 自动行尾转换提示 |
| 连续创作测试 | 10 项通过，覆盖五类型多轮生成、实际图片、分支、恢复、源码与预览路径 |
| stdio MCP | 4 项通过，包含新增工具调用及真实源码再次打包 |

全量后补充了旧 websiteRequest 按网站类型恢复的兼容检查；连续创作 10 项再次通过，lint、typecheck、build 也再次通过。

全量最终日志：`work/workflow-final-tests.log`。两项已在本轮改动前复现的失败保持原断言：

- `tests/issue-regressions.test.mjs:41` 创建 Windows 目录符号链接报 EPERM。
- `tests/service-settings.test.mjs:19` Windows 文件权限位为 0666，断言要求 POSIX 0600。

回归中发现一处测试等待竞争：任务进入 waiting_external 时，dispatch 可能仍为 pending。测试现在等待 manual 交接状态后检查，保留 manual 断言，没有将 pending 当作成功。

## 隔离浏览器与文件检查

测试使用 `work/workflow-qa-20260908/data`、Runtime 8792、前端 3002；模拟适配器禁止真实外部请求，五个测试项目未写入真实数据目录。夹具中的纯色图片和 FFmpeg 视频只用于传输验证。

- 视频：画廊选择实际概念图，多选两张图准备首帧；图片先加入候选、再采用，随后生成并采用 5 秒 MP4。记录确认收到两张参考；视频请求图片字节与页面采用的 PNG 完全相同。
- 类型：视频切换小说后不自动复制对象或美术；显式沿用一个人物和原文本作为适配资料。切回视频后首帧、参数和已采用 MP4 仍在；保存文件中各分支独立。
- 小说：从正文生成 characters/world 建议，仅勾选人物后采用；人物小节更新，背景仍是原文。对象更新建议只采用人物一项，另一对象描述和两张已保存图均保留，没有新增同名对象。
- 网站：导入候选并采用，下一轮任务的 existing-website.zip 与当前采用源码逐字节一致，PROMPT.md 与保存提示词一致。另通过浏览器真实文件选择器上传 ZIP，出现新候选及运行说明字段，未自动替换当前源码。
- 静态预览：包内 HTML、外部 CSS 和 JavaScript 实际加载，按钮从“显示全部 Q-027”切换为“仅显示旧伞 Q-027”。预览仍为 sandbox allow-scripts。开发环境使用限定于 GET／不可变源码路径的静态文件处理器，其他页面、项目和写操作仍经过原来源保护；编码路径穿越测试拒绝转成工作区读取。
- 3D 前期：浏览器生成并采用设计资料包，实际 ZIP 包含 DESIGN.json、README.md、SOURCES.json 和两张 PNG；交付要求中保留 Q-027，并明确没有生成模型。
- 桌面截图核对了对象卡、选定参考区和源码静态样式；本记录不代表完整移动端、真实生成质量或可用性研究。

同时修复了重复 React key 导致依据面板随保存重复显示、小说选择入口误留在演示分支、对象响应未返回 usage 时清空原用途的问题。恢复旧成果保留原始依赖，不把旧版本静默标为当前依据；现存镜头／对象恢复不改变排列位置。

## 接入与验证边界

共享 Runtime 已按相同真实数据目录更新到核心协议 4；前端 3001 可访问。两项 Skills 为 0.3.0，清单登记 19 个工具和新增任务／文件能力。`npm run workbench:setup` 重新生成 Skill ZIP、MCP 配置及接入／验收说明，包内规则与源文件一致。

未调用真实 DeepSeek、WorkBuddy、Runway 账户，没有验证媒体质量；未实际在 WorkBuddy 客户端安装／触发 Skills。网站执行端的完整编程与构建仍需在真实任务中验收，本轮只验证真实源码交接和静态夹具交互。长篇编排和真实 3D 模型生成继续留在后续范围。隔离 QA 进程和浏览器标签在验收后关闭，原测试资料保留。
