# WorkBuddy 实际验收反馈修复与复测

日期：2026-09-09。依据用户提供的实际验收记录及 `website-source.zip`。本轮修改基于 `19b4f73`，未提交或推送 Git。另一任务“修复生成导致电脑卡死”在同一工作区修改运行与资源控制；本记录只归纳本轮验收修复，完整测试包含当时两边的代码。

## 已实现

| 反馈 | 修复结果 | 证据边界 |
| --- | --- | --- |
| WorkBuddy 部分数组必须通过 HTTP 绕行 | MCP 传输层兼容原生数组和一层 JSON 数组字符串；新增受限 `workbench_call_json`，只路由已有核心工具，保留严格字段、版本冲突和同 ID 重放规则 | 真实 stdio MCP 客户端测试通过；不能据此确认 WorkBuddy 客户端自身故障根因或本版加载通过 |
| 16:9 视频实际为 1:1 | 生成前读取实际首帧尺寸；不匹配时先拦截。新增补边／居中裁切真实 PNG，保留原图。收到 MP4 后记录宽高、显示比例、时长，与请求分别比较；不合格文件保留，拒绝作为合格生成镜头采用 | 本地实际 PNG/MP4 验证；不承诺外部视频服务一定遵循要求。手动导入的旧片段保留并显示规格结果，缺少元数据为未验证 |
| FAQ 再次展开失败 | 修复用户提供的实际源码，清除残留高度限制、延迟隐藏和键盘重复触发；答案使用 hidden 即时切换 | 实际 Chromium 浏览器点击复现与复测，详见下文 |
| 静态测试误判网站通过 | 网站源码记录分开保存验证方式、结果和证据。ZIP 可读取、静态检查或 HTTP 200 不提升为浏览器通过；外部报告通过也明确不是 Runtime 独立验证，失败记录使交付标为部分完成 | 源码导入、采用、恢复、交付记录测试；网页分别测试静态通过与浏览器失败显示 |
| 小说导出标题重复 | Web 与 MCP 使用同一导出处理，仅移除正文开头与标题字段相同的独立标题行；保存的原稿和文中同名引用保留。Skills 明确梗概与正文的不同字段 | TXT/Markdown 原文保留及标题处理回归测试；当前对话写入仍无需额外文字 API |
| 返工改动不明显 | 增加原图／候选并排对比、同步放大、指定变化与保留项核对、观察记录；选用修改图前在页面确认。保留图片祖先，避免对比原图被清理。编辑提示词明确位置、变化和程度 | 页面用固定色块验证交互与保存，不是 AI 效果验收；没有追加付费生成 |

网页新增首帧操作只开放所需的 `project_get` / `video_frame_fit` 同源代理，未把 JSON 兼容入口开放为网页任意调用。生成内容、草稿和外部文件仍是数据，不构成操作权限。

## 网站原件及修复包

交付文件位于仓库 `work/acceptance-fixes-20260909/website-source-fixed.zip`。内含原 `index.html`、修复后的 `styles.css` / `script.js` 和更新的 `README.md`。文案与其余布局沿用原件，本轮没有声称逐项重新核验网站文案。

- 原 ZIP SHA-256：`87fbc1e7f7910a9e60c8e73bd5d27d5269dca3eadf7c0f6c8eb23e07cd4a7a0e`。
- 修复 ZIP SHA-256：`14b921778e88e10247312c4a7fb1e51c3b1cc64452775b3f9dc83e3cd310a6b7`。
- 原 ZIP 和原始解包内容逐字节核对未改变；修复包中源码与浏览器复测文件一致。

在本机静态服务、Codex 内嵌 Chromium 中验证：

1. 原件第一项 FAQ 再次展开时 `aria-expanded=true`，实际区域高度 32px，内容需 91px，`max-height:0px` 残留，确认答案被裁切。
2. 修复版六个问题逐项展开→收起→再次展开，展开时内容高度完整（91/118px），收起为 0px。
3. 原生连续双击后状态一致；六项能独立同时展开；Enter 收起、Space 再次展开。
4. 390×844 手机视口下答案高度 146–200px，与内容高度一致，无横向溢出；重复切换正常。
5. 实际点击四个导航链接，分别到达 `#home`、`#craft`、`#steps`、`#faq`。

证据：同目录 `browser-evidence.json`、`faq-desktop.png`、`faq-mobile.png`。没有使用 `__siteApi`、模拟 DOM 或仅检查 aria 状态作为通过依据；未覆盖其他浏览器与公网部署。

## 工作台与协议测试

- 隔离数据目录 `work/ui-verification`、Runtime 8792、网页 3002：返工确认按钮在未核对或无记录时不可用；填写观察后选用并保存，原图仍在。同步放大可操作。
- 页面实际点击补边，首帧 PNG 从 400×400 变为 1280×720，原图保留。测试发现并修复网页代理未接入新工具的问题。
- 网站填“静态检查通过”仍显示浏览器未验证；填实际浏览器失败与证据后显示执行端报告失败，保存后可读回。
- 实际本地 MP4：方图输入在服务请求前拦截；方形输出保留但拒绝采用；320×180 合规片段可采用；改变目标画幅后交付重新报告不匹配。容差为显示比例相对误差 1%、时长差 0.25 秒。
- MCP 原生／编码数组、嵌套数组、错误类型、JSON 兼容入口、同 ID 重放、未知字段与递归调用拒绝均测试。没有凭证或真实外部生成调用。
- 完整测试最近一次：107 项，105 通过，2 失败。失败为既有 Windows 环境问题：`issue-regressions.test.mjs` 创建符号链接 EPERM；`service-settings.test.mjs` POSIX 0600 文件权限断言。未修改这两项或将其计为通过。最终网站报告状态调整另做针对性测试通过。
- lint、typecheck、build 通过。日志分别见同目录 `tests-final.log`、`website-final-tests.log`、`build.log`。浏览器侧测试不等于 WorkBuddy 内实测或真实模型质量验收。

## WorkBuddy 更新与待复测

当前核心协议 **6**、工具 **23**、两项 Skills **0.5.0**。已重新生成 `work/workbuddy-core` 中的两项 Skill ZIP、`mcp.json`、说明和验收清单，校对 ZIP 与源码一致、引用齐全、清单与实际工具一致。

通用 Skill 验证脚本因本机 Python 缺少 `yaml` 未运行成功；保留现有 WorkBuddy 扩展前置信息，另做了名称、版本、引用、ZIP 及配置检查，不宣称通用脚本通过。记录为 `package-verification.json`。

用户接下来停止旧 Runtime，在 WorkBuddy 重新导入两项 Skills 与连接配置，确认协议 6 和 23 工具，然后执行[验收目录 L01–L08](创意工作台验收条目.md)。重点是 WorkBuddy 实际数组调用、真实视频画幅、真实图片返工效果和导入修复 ZIP 后的浏览器操作。本轮不覆盖旧工作区文件，不导入游戏原项目，不扩展 3D 范围。
