// Shared release version for the MCP server and both installable Skills.
// After changing this value, run npm run workbench:setup to sync metadata.
// CORE_PROTOCOL is a separate integer compatibility revision, not this version.
export const WORKBENCH_VERSION = '0.11.0';
